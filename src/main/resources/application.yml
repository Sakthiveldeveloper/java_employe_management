spring:
  application:
    name: employee-management

  datasource:
    url: jdbc:mysql://localhost:3306/employee_management_db
    username: root
    password: Admin@sakthi123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080